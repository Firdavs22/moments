import 'package:flutter/material.dart';

class CreatePostScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Создать пост')),
      body: Center(
        child: Text('Здесь будет экран создания поста'),
      ),
    );
  }
}
